{
  "unstable": ["kv"],
  "tasks": {
    "dev": "deno run --allow-net --allow-env --allow-read --allow-write --unstable-kv --watch src/index.ts",
    "start": "deno run --allow-net --allow-env --allow-read --allow-write --unstable-kv src/index.ts",
    "deploy": "bash scripts/deploy.sh",
    "deploy:check": "deno run --allow-read --allow-env --allow-net --allow-run=git scripts/check_deployment.ts",
    "build-info": "deno run --allow-read --allow-write --allow-run=git scripts/build_info.ts",
    "test": "deno test --allow-read --allow-run=git src scripts"
  },
  "lint": {
    "exclude": ["frontend"],
    "rules": {
      "tags": ["recommended"]
    }
  },
  "fmt": {
    "exclude": ["frontend"],
    "useTabs": false,
    "lineWidth": 100,
    "indentWidth": 2,
    "semiColons": true,
    "singleQuote": false,
    "proseWrap": "preserve"
  },
  "compilerOptions": {
    "lib": ["deno.window", "deno.unstable"]
  },
  "imports": {
    "hono": "jsr:@hono/hono@^4.6.0",
    "hono/": "jsr:/@hono/hono@^4.6.0/"
  },
  "deploy": {
    "org": "dandibbert",
    "app": "pixvel"
  }
}
